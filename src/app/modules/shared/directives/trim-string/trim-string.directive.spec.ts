import { TrimStringDirective } from './trim-string.directive';

describe('TrimStringDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimStringDirective();
    expect(directive).toBeTruthy();
  });
});
